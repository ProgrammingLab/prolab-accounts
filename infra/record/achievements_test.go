// Code generated by SQLBoiler (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package record

import (
	"bytes"
	"context"
	"reflect"
	"testing"

	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/randomize"
	"github.com/volatiletech/sqlboiler/strmangle"
)

var (
	// Relationships sometimes use the reflection helper queries.Equal/queries.Assign
	// so force a package dependency in case they don't.
	_ = queries.Equal
)

func testAchievements(t *testing.T) {
	t.Parallel()

	query := Achievements()

	if query.Query == nil {
		t.Error("expected a query, got nothing")
	}
}

func testAchievementsDelete(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := o.Delete(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAchievementsQueryDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if rowsAff, err := Achievements().DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAchievementsSliceDeleteAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AchievementSlice{o}

	if rowsAff, err := slice.DeleteAll(ctx, tx); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only have deleted one row, but affected:", rowsAff)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 0 {
		t.Error("want zero records, got:", count)
	}
}

func testAchievementsExists(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	e, err := AchievementExists(ctx, tx, o.ID)
	if err != nil {
		t.Errorf("Unable to check if Achievement exists: %s", err)
	}
	if !e {
		t.Errorf("Expected AchievementExists to return true, but got false.")
	}
}

func testAchievementsFind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	achievementFound, err := FindAchievement(ctx, tx, o.ID)
	if err != nil {
		t.Error(err)
	}

	if achievementFound == nil {
		t.Error("want a record, got nil")
	}
}

func testAchievementsBind(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = Achievements().Bind(ctx, tx, o); err != nil {
		t.Error(err)
	}
}

func testAchievementsOne(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if x, err := Achievements().One(ctx, tx); err != nil {
		t.Error(err)
	} else if x == nil {
		t.Error("expected to get a non nil record")
	}
}

func testAchievementsAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	achievementOne := &Achievement{}
	achievementTwo := &Achievement{}
	if err = randomize.Struct(seed, achievementOne, achievementDBTypes, false, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}
	if err = randomize.Struct(seed, achievementTwo, achievementDBTypes, false, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = achievementOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = achievementTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Achievements().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 2 {
		t.Error("want 2 records, got:", len(slice))
	}
}

func testAchievementsCount(t *testing.T) {
	t.Parallel()

	var err error
	seed := randomize.NewSeed()
	achievementOne := &Achievement{}
	achievementTwo := &Achievement{}
	if err = randomize.Struct(seed, achievementOne, achievementDBTypes, false, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}
	if err = randomize.Struct(seed, achievementTwo, achievementDBTypes, false, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = achievementOne.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}
	if err = achievementTwo.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 2 {
		t.Error("want 2 records, got:", count)
	}
}

func achievementBeforeInsertHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementAfterInsertHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementAfterSelectHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementBeforeUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementAfterUpdateHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementBeforeDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementAfterDeleteHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementBeforeUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func achievementAfterUpsertHook(ctx context.Context, e boil.ContextExecutor, o *Achievement) error {
	*o = Achievement{}
	return nil
}

func testAchievementsHooks(t *testing.T) {
	t.Parallel()

	var err error

	ctx := context.Background()
	empty := &Achievement{}
	o := &Achievement{}

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, o, achievementDBTypes, false); err != nil {
		t.Errorf("Unable to randomize Achievement object: %s", err)
	}

	AddAchievementHook(boil.BeforeInsertHook, achievementBeforeInsertHook)
	if err = o.doBeforeInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeInsertHook function to empty object, but got: %#v", o)
	}
	achievementBeforeInsertHooks = []AchievementHook{}

	AddAchievementHook(boil.AfterInsertHook, achievementAfterInsertHook)
	if err = o.doAfterInsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterInsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterInsertHook function to empty object, but got: %#v", o)
	}
	achievementAfterInsertHooks = []AchievementHook{}

	AddAchievementHook(boil.AfterSelectHook, achievementAfterSelectHook)
	if err = o.doAfterSelectHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterSelectHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterSelectHook function to empty object, but got: %#v", o)
	}
	achievementAfterSelectHooks = []AchievementHook{}

	AddAchievementHook(boil.BeforeUpdateHook, achievementBeforeUpdateHook)
	if err = o.doBeforeUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpdateHook function to empty object, but got: %#v", o)
	}
	achievementBeforeUpdateHooks = []AchievementHook{}

	AddAchievementHook(boil.AfterUpdateHook, achievementAfterUpdateHook)
	if err = o.doAfterUpdateHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpdateHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpdateHook function to empty object, but got: %#v", o)
	}
	achievementAfterUpdateHooks = []AchievementHook{}

	AddAchievementHook(boil.BeforeDeleteHook, achievementBeforeDeleteHook)
	if err = o.doBeforeDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeDeleteHook function to empty object, but got: %#v", o)
	}
	achievementBeforeDeleteHooks = []AchievementHook{}

	AddAchievementHook(boil.AfterDeleteHook, achievementAfterDeleteHook)
	if err = o.doAfterDeleteHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterDeleteHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterDeleteHook function to empty object, but got: %#v", o)
	}
	achievementAfterDeleteHooks = []AchievementHook{}

	AddAchievementHook(boil.BeforeUpsertHook, achievementBeforeUpsertHook)
	if err = o.doBeforeUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doBeforeUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected BeforeUpsertHook function to empty object, but got: %#v", o)
	}
	achievementBeforeUpsertHooks = []AchievementHook{}

	AddAchievementHook(boil.AfterUpsertHook, achievementAfterUpsertHook)
	if err = o.doAfterUpsertHooks(ctx, nil); err != nil {
		t.Errorf("Unable to execute doAfterUpsertHooks: %s", err)
	}
	if !reflect.DeepEqual(o, empty) {
		t.Errorf("Expected AfterUpsertHook function to empty object, but got: %#v", o)
	}
	achievementAfterUpsertHooks = []AchievementHook{}
}

func testAchievementsInsert(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAchievementsInsertWhitelist(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Whitelist(achievementColumnsWithoutDefault...)); err != nil {
		t.Error(err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}
}

func testAchievementToManyAchievementUsers(t *testing.T) {
	var err error
	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Achievement
	var b, c AchievementUser

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	if err = randomize.Struct(seed, &b, achievementUserDBTypes, false, achievementUserColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}
	if err = randomize.Struct(seed, &c, achievementUserDBTypes, false, achievementUserColumnsWithDefault...); err != nil {
		t.Fatal(err)
	}

	b.AchievementID = a.ID
	c.AchievementID = a.ID

	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	check, err := a.AchievementUsers().All(ctx, tx)
	if err != nil {
		t.Fatal(err)
	}

	bFound, cFound := false, false
	for _, v := range check {
		if v.AchievementID == b.AchievementID {
			bFound = true
		}
		if v.AchievementID == c.AchievementID {
			cFound = true
		}
	}

	if !bFound {
		t.Error("expected to find b")
	}
	if !cFound {
		t.Error("expected to find c")
	}

	slice := AchievementSlice{&a}
	if err = a.L.LoadAchievementUsers(ctx, tx, false, (*[]*Achievement)(&slice), nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.AchievementUsers); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	a.R.AchievementUsers = nil
	if err = a.L.LoadAchievementUsers(ctx, tx, true, &a, nil); err != nil {
		t.Fatal(err)
	}
	if got := len(a.R.AchievementUsers); got != 2 {
		t.Error("number of eager loaded records wrong, got:", got)
	}

	if t.Failed() {
		t.Logf("%#v", check)
	}
}

func testAchievementToManyAddOpAchievementUsers(t *testing.T) {
	var err error

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()

	var a Achievement
	var b, c, d, e AchievementUser

	seed := randomize.NewSeed()
	if err = randomize.Struct(seed, &a, achievementDBTypes, false, strmangle.SetComplement(achievementPrimaryKeyColumns, achievementColumnsWithoutDefault)...); err != nil {
		t.Fatal(err)
	}
	foreigners := []*AchievementUser{&b, &c, &d, &e}
	for _, x := range foreigners {
		if err = randomize.Struct(seed, x, achievementUserDBTypes, false, strmangle.SetComplement(achievementUserPrimaryKeyColumns, achievementUserColumnsWithoutDefault)...); err != nil {
			t.Fatal(err)
		}
	}

	if err := a.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = b.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}
	if err = c.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Fatal(err)
	}

	foreignersSplitByInsertion := [][]*AchievementUser{
		{&b, &c},
		{&d, &e},
	}

	for i, x := range foreignersSplitByInsertion {
		err = a.AddAchievementUsers(ctx, tx, i != 0, x...)
		if err != nil {
			t.Fatal(err)
		}

		first := x[0]
		second := x[1]

		if a.ID != first.AchievementID {
			t.Error("foreign key was wrong value", a.ID, first.AchievementID)
		}
		if a.ID != second.AchievementID {
			t.Error("foreign key was wrong value", a.ID, second.AchievementID)
		}

		if first.R.Achievement != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}
		if second.R.Achievement != &a {
			t.Error("relationship was not added properly to the foreign slice")
		}

		if a.R.AchievementUsers[i*2] != first {
			t.Error("relationship struct slice not set to correct value")
		}
		if a.R.AchievementUsers[i*2+1] != second {
			t.Error("relationship struct slice not set to correct value")
		}

		count, err := a.AchievementUsers().Count(ctx, tx)
		if err != nil {
			t.Fatal(err)
		}
		if want := int64((i + 1) * 2); count != want {
			t.Error("want", want, "got", count)
		}
	}
}

func testAchievementsReload(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	if err = o.Reload(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAchievementsReloadAll(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice := AchievementSlice{o}

	if err = slice.ReloadAll(ctx, tx); err != nil {
		t.Error(err)
	}
}

func testAchievementsSelect(t *testing.T) {
	t.Parallel()

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	slice, err := Achievements().All(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if len(slice) != 1 {
		t.Error("want one record, got:", len(slice))
	}
}

var (
	achievementDBTypes = map[string]string{`ID`: `bigint`, `Title`: `character varying`, `Award`: `character varying`, `URL`: `character varying`, `Description`: `text`, `ImageFilename`: `character varying`, `HappenedAt`: `timestamp without time zone`, `CreatedAt`: `timestamp without time zone`, `UpdatedAt`: `timestamp without time zone`}
	_                  = bytes.MinRead
)

func testAchievementsUpdate(t *testing.T) {
	t.Parallel()

	if 0 == len(achievementPrimaryKeyColumns) {
		t.Skip("Skipping table with no primary key columns")
	}
	if len(achievementColumns) == len(achievementPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	if rowsAff, err := o.Update(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("should only affect one row but affected", rowsAff)
	}
}

func testAchievementsSliceUpdateAll(t *testing.T) {
	t.Parallel()

	if len(achievementColumns) == len(achievementPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	o := &Achievement{}
	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementColumnsWithDefault...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Insert(ctx, tx, boil.Infer()); err != nil {
		t.Error(err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}

	if count != 1 {
		t.Error("want one record, got:", count)
	}

	if err = randomize.Struct(seed, o, achievementDBTypes, true, achievementPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	// Remove Primary keys and unique columns from what we plan to update
	var fields []string
	if strmangle.StringSliceMatch(achievementColumns, achievementPrimaryKeyColumns) {
		fields = achievementColumns
	} else {
		fields = strmangle.SetComplement(
			achievementColumns,
			achievementPrimaryKeyColumns,
		)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	typ := reflect.TypeOf(o).Elem()
	n := typ.NumField()

	updateMap := M{}
	for _, col := range fields {
		for i := 0; i < n; i++ {
			f := typ.Field(i)
			if f.Tag.Get("boil") == col {
				updateMap[col] = value.Field(i).Interface()
			}
		}
	}

	slice := AchievementSlice{o}
	if rowsAff, err := slice.UpdateAll(ctx, tx, updateMap); err != nil {
		t.Error(err)
	} else if rowsAff != 1 {
		t.Error("wanted one record updated but got", rowsAff)
	}
}

func testAchievementsUpsert(t *testing.T) {
	t.Parallel()

	if len(achievementColumns) == len(achievementPrimaryKeyColumns) {
		t.Skip("Skipping table with only primary key columns")
	}

	seed := randomize.NewSeed()
	var err error
	// Attempt the INSERT side of an UPSERT
	o := Achievement{}
	if err = randomize.Struct(seed, &o, achievementDBTypes, true); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	ctx := context.Background()
	tx := MustTx(boil.BeginTx(ctx, nil))
	defer func() { _ = tx.Rollback() }()
	if err = o.Upsert(ctx, tx, false, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Achievement: %s", err)
	}

	count, err := Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}

	// Attempt the UPDATE side of an UPSERT
	if err = randomize.Struct(seed, &o, achievementDBTypes, false, achievementPrimaryKeyColumns...); err != nil {
		t.Errorf("Unable to randomize Achievement struct: %s", err)
	}

	if err = o.Upsert(ctx, tx, true, nil, boil.Infer(), boil.Infer()); err != nil {
		t.Errorf("Unable to upsert Achievement: %s", err)
	}

	count, err = Achievements().Count(ctx, tx)
	if err != nil {
		t.Error(err)
	}
	if count != 1 {
		t.Error("want one record, got:", count)
	}
}
